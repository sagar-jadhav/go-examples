package main

import "fmt"

/*
 * string-length program demonstrates how to calculate
 * length of string
 */
func main() {
	repositoryName := "go-examples"
	fmt.Println("Length of", repositoryName, "string is", len(repositoryName))
}
